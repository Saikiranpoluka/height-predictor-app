# height-predictor-app
An interactive Height Prediction web app built with a 2nd-degree Polynomial Regression model (92.76% accuracy). Features data scaling via Scikit-Learn pipelines, robust 5-Fold Cross-Validation verification (79.71% stable baseline), and a production-ready cloud dashboard deployed using Streamlit.
